{
  "name": "cannon",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://danhemerlein.github.io/cannon/",
  "dependencies": {
    "classnames": "^2.2.6",
    "contentful": "^7.5.0",
    "gh-pages": "^2.0.1",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "NODE_PATH=src react-scripts start",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "build": "NODE_PATH=src react-scripts build",
    "test": "NODE_PATH=src react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "basement": "https://github.com/sanctuarycomputer/basement.git"
  }
}
